import ui, {a, b} from './module1.mjs'
// we can put any name after import as it is a default export
// but if we import without default command we have to give specific name 
console.log(ui);
console.log(b);